#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../yo/bin/yo" "$@"
  ret=$?
else 
  node  "$basedir/../yo/bin/yo" "$@"
  ret=$?
fi
exit $ret
